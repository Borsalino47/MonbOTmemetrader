services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      CP_ENVIRONMENT: prod
      CP_LOG_JSON: "true"
      CP_PAPER_MODE: "true"
      CP_DB_URL: postgresql+psycopg://cryptopulse:cryptopulse@db:5432/cryptopulse
    env_file:
      - path: .env
        required: false
    depends_on:
      db:
        condition: service_healthy
    volumes:
      # The alert journal (data/alerts.jsonl) is a record independent of the
      # database. Losing it when the container is replaced would lose the one
      # copy that survives a database problem.
      - radardata:/app/data
    restart: unless-stopped
    stop_grace_period: 30s

  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: cryptopulse
      POSTGRES_PASSWORD: cryptopulse
      POSTGRES_DB: cryptopulse
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U cryptopulse"]
      interval: 5s
      timeout: 5s
      retries: 10
    restart: unless-stopped

volumes:
  pgdata:
  radardata:
